{% extends 'base.html.twig' %}

{% block body %}
    <!-- gallery start -->
    <section id="gallery" class="gallery lightsmoke">
        <div class="container">
            <div class="row">
                <div class="col-sm-12 text-center">
                    <div class="heading wow animated fadeInUp">
                        <h3>{{ text_helper.getWebSiteText('gallerie-photo').title }}</h3>
                        {{ text_helper.getWebSiteText('gallerie-photo').description|raw }}
                    </div>
                    <!-- /.heading end -->
                </div>
            </div>
            <div class="color-overlay">
                <div class="container">
                    <div class="row">
                        <div class="col-sm-12 text-center">
                            <a href={{ path('wedding_picture', {folder:'mairie'}) }} class="btn default-btn">Mairie</a>
                            <a href={{ path('wedding_picture', {folder:'espagne'}) }} class="btn default-btn">Espagne -
                            Fabrice H.</a>
                            <a href={{ path('wedding_picture', {folder:'jp'}) }} class="btn default-btn">'Espagne -
                            JP</a>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.color-overlay end -->
            <div class="ajax-load">
                {% include ":default/partials:picture.html.twig" %}
            </div>
        </div>
    </section>
    <!-- gallery end -->
{% endblock %}

{% block javascripts %}
    <script charset="UTF-8">
        $(document).ready(function () {
            var win = $(window);
            var $footerOffsetTop = $('[data-footer]').offset().top;
            var $sticker = $('#sticker').outerHeight();

            console.log($sticker);
            // Each time the user scrolls
            win.scroll(function () {
                var scrollTop = $(this).scrollTop();
                var winHeight = $(this).height();

                var total = scrollTop + winHeight + $sticker;
console.log(total);

                //console.log(scrollTop + $(this).height() + $sticker);
                //if (scrollTop + $(this).height() + $sticker > $footerOffsetTop) {
                  //  console.log('fdfsdfs');
                    //console.log(scrollTop+' / '+$footerOffsetTop);
//                    $('#loading').show();
//                    $.ajax({
//                        url: 'get-post.php',
//                        dataType: 'html',
//                        success: function (html) {
//                            $('#posts').append(html);
//                            $('#loading').hide();
//                        },
//                    });
                //}



            });
//                // End of the document reached?
//                if ($(document).height() - win.height() == win.scrollTop()) {
//                console.log(win.height());
//                console.log($(document).height());
//                console.log(offsetTop());
//                }
        });
    </script>
{% endblock %}
